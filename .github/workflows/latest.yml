name: Agregarr Release

on:
  workflow_dispatch:
  push:
    branches:
      - latest

jobs:
<<<<<<< HEAD
  semantic-release:
    name: Tag and create release
    runs-on: ubuntu-22.04
    permissions:
      contents: write
      issues: write
      pull-requests: write
=======
  version-check:
    name: Check for new version
    runs-on: ubuntu-22.04
    permissions:
      contents: write
>>>>>>> upstream/latest
    outputs:
      new_release_published: ${{ steps.semantic.outputs.new_release_published }}
      new_release_version: ${{ steps.semantic.outputs.new_release_version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
<<<<<<< HEAD
=======
          cache: 'yarn'
>>>>>>> upstream/latest
      - name: Install dependencies
        env:
          HUSKY: 0
        run: yarn
<<<<<<< HEAD
      - name: Release
        id: semantic
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          npx semantic-release
          echo "new_release_published=$(cat .release-status 2>/dev/null || echo false)" >> $GITHUB_OUTPUT
          echo "new_release_version=$(cat .release-version 2>/dev/null || echo '')" >> $GITHUB_OUTPUT

  build_and_push:
    name: Build & Publish Docker Images
    needs: semantic-release
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-latest
          - platform: linux/arm64
            runner: [self-hosted, linux, ARM64]
    runs-on: ${{ matrix.runner }}
    permissions:
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.semantic-release.outputs.new_release_version && format('v{0}', needs.semantic-release.outputs.new_release_version) || github.ref }}
      - name: Convert repository name to lowercase
        id: repo
        run: echo "repository=${GITHUB_REPOSITORY,,}" >> $GITHUB_OUTPUT
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
=======
      - name: Check version (dry-run)
        id: semantic
        uses: cycjimmy/semantic-release-action@v4
        with:
          dry_run: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build_and_push:
    name: Build ${{ matrix.platform }}
    runs-on: ${{ matrix.runner }}
    needs: version-check
    if: needs.version-check.outputs.new_release_published == 'true'
    permissions:
      packages: write
      contents: read
    strategy:
      matrix:
        include:
          - platform: linux/amd64
            runner: ubuntu-22.04
          - platform: linux/arm64
            runner: [self-hosted, linux, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Update version in package.json
        run: npm version ${{ needs.version-check.outputs.new_release_version }} --no-git-tag-version

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

>>>>>>> upstream/latest
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
<<<<<<< HEAD
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.repo.outputs.repository }}
          tags: |
            ${{ needs.semantic-release.outputs.new_release_version && format('type=semver,pattern={{{{version}}}},value={0}', needs.semantic-release.outputs.new_release_version) || '' }}
            ${{ needs.semantic-release.outputs.new_release_version && format('type=semver,pattern={{{{major}}}}.{{{{minor}}}},value={0}', needs.semantic-release.outputs.new_release_version) || '' }}
            ${{ needs.semantic-release.outputs.new_release_version && format('type=semver,pattern={{{{major}}}},value={0}', needs.semantic-release.outputs.new_release_version) || '' }}
            type=raw,value=latest
=======

>>>>>>> upstream/latest
      - name: Build and push by digest
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
<<<<<<< HEAD
          file: ./Dockerfile
          platforms: ${{ matrix.platform }}
          outputs: type=image,name=ghcr.io/${{ steps.repo.outputs.repository }},push-by-digest=true,name-canonical=true,push=true
          build-args: |
            COMMIT_TAG=${{ github.sha }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Export digest
        run: |
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"
=======
          platforms: ${{ matrix.platform }}
          build-args: |
            COMMIT_TAG=${{ github.sha }}
          outputs: type=image,name=ghcr.io/agregarr/agregarr,push-by-digest=true,name-canonical=true,push=true

      - name: Export digest
        run: |
          rm -rf /tmp/digests
          mkdir -p /tmp/digests
          digest="${{ steps.build.outputs.digest }}"
          touch "/tmp/digests/${digest#sha256:}"

>>>>>>> upstream/latest
      - name: Upload digest
        uses: actions/upload-artifact@v4
        with:
          name: digests-${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
          path: /tmp/digests/*
          if-no-files-found: error
          retention-days: 1

  merge:
<<<<<<< HEAD
    name: Merge multi-arch manifest
    runs-on: ubuntu-latest
    needs: [semantic-release, build_and_push]
    permissions:
      packages: write
=======
    name: Create multi-platform manifest
    runs-on: ubuntu-22.04
    needs:
      - version-check
      - build_and_push
    permissions:
      packages: write
      contents: read
>>>>>>> upstream/latest
    steps:
      - name: Download digests
        uses: actions/download-artifact@v4
        with:
<<<<<<< HEAD
          path: /tmp/digests
          pattern: digests-*
          merge-multiple: true
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
=======
          pattern: digests-*
          path: /tmp/digests
          merge-multiple: true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

>>>>>>> upstream/latest
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}
<<<<<<< HEAD
      - name: Convert repository name to lowercase
        id: repo
        run: echo "repository=${GITHUB_REPOSITORY,,}" >> $GITHUB_OUTPUT
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.repo.outputs.repository }}
          tags: |
            ${{ needs.semantic-release.outputs.new_release_version && format('type=semver,pattern={{{{version}}}},value={0}', needs.semantic-release.outputs.new_release_version) || '' }}
            ${{ needs.semantic-release.outputs.new_release_version && format('type=semver,pattern={{{{major}}}}.{{{{minor}}}},value={0}', needs.semantic-release.outputs.new_release_version) || '' }}
            ${{ needs.semantic-release.outputs.new_release_version && format('type=semver,pattern={{{{major}}}},value={0}', needs.semantic-release.outputs.new_release_version) || '' }}
            type=raw,value=latest
      - name: Create manifest list and push
        working-directory: /tmp/digests
        run: |
          docker buildx imagetools create $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
            $(printf 'ghcr.io/${{ steps.repo.outputs.repository }}@sha256:%s ' *)
      - name: Inspect image
        run: |
          docker buildx imagetools inspect ghcr.io/${{ steps.repo.outputs.repository }}:latest
=======

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Create manifest on GHCR
        working-directory: /tmp/digests
        run: |
          docker buildx imagetools create \
            -t ghcr.io/agregarr/agregarr:latest \
            -t ghcr.io/agregarr/agregarr:v${{ needs.version-check.outputs.new_release_version }} \
            $(printf 'ghcr.io/agregarr/agregarr@sha256:%s ' *)

      - name: Install regctl
        run: |
          curl -L https://github.com/regclient/regclient/releases/latest/download/regctl-linux-amd64 >/tmp/regctl
          chmod +x /tmp/regctl

      - name: Copy to Docker Hub
        run: |
          /tmp/regctl image copy ghcr.io/agregarr/agregarr:latest agregarr/agregarr:latest
          /tmp/regctl image copy ghcr.io/agregarr/agregarr:v${{ needs.version-check.outputs.new_release_version }} agregarr/agregarr:v${{ needs.version-check.outputs.new_release_version }}

      - name: Inspect images
        run: |
          docker buildx imagetools inspect ghcr.io/agregarr/agregarr:latest
          docker buildx imagetools inspect agregarr/agregarr:latest

  release:
    name: Create release
    runs-on: ubuntu-22.04
    needs:
      - version-check
      - merge
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'yarn'
      - name: Install dependencies
        env:
          HUSKY: 0
        run: yarn
      - name: Release
        uses: cycjimmy/semantic-release-action@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  discord:
    name: Discord Notification
    runs-on: ubuntu-22.04
    needs:
      - version-check
      - release
    steps:
      - name: Send Discord Notification
        run: |
          curl -H "Content-Type: application/json" \
            -d '{
              "content": "@everyone",
              "embeds": [{
                "title": "🎉 Agregarr v${{ needs.version-check.outputs.new_release_version }} Released!",
                "description": "A new version of Agregarr is now available!\n\n**[📝 View Release Notes](${{ github.server_url }}/${{ github.repository }}/releases/tag/v${{ needs.version-check.outputs.new_release_version }})**",
                "color": 15105570,
                "fields": [
                  {"name": "🐳 Update your Docker image", "value": "```docker pull agregarr/agregarr:latest```", "inline": false}
                ],
                "thumbnail": {"url": "https://raw.githubusercontent.com/agregarr/agregarr/develop/public/android-chrome-512x512.png"}
              }]
            }' \
            ${{ secrets.DISCORD_WEBHOOK_LATEST }}
>>>>>>> upstream/latest
